# ---------------------------------------------------------------------------
# ข้อมูลลับ - ห้าม commit เด็ดขาด
# .env เก็บ OpenAI API Key ตัวจริง ถ้าหลุดขึ้น git จะถูกนำไปใช้ทันที
# ---------------------------------------------------------------------------
.env
*.dpapi
config/team.ps1
config/tunnel-client.yaml

# ---------------------------------------------------------------------------
# tunnel-client ถูกเก็บไว้ใน repo ด้วย เพื่อให้ Code > Download ZIP
# ได้ไฟล์ครบใช้งานได้ทันที ไม่ต้องโหลดเพิ่ม
# ---------------------------------------------------------------------------
*.zip

# ---------------------------------------------------------------------------
# ไฟล์ชั่วคราว / เครื่องมือภายใน
# ---------------------------------------------------------------------------
.freebuff/
_*.ps1
*.bak
*.log
