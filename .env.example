# ===================================================================
# ตัวอย่างไฟล์ตั้งค่า
# ให้คัดลอกไฟล์นี้เป็นชื่อ  .env  แล้วใส่ค่าจริงของคุณ
# ห้าม commit ไฟล์ .env ขึ้น git เด็ดขาด เพราะมี API Key อยู่
# ===================================================================

openai api key : <<< วาง OpenAI API Key ของคุณตรงนี้ >>>

tunnel ID : <<< วาง Tunnel ID ของคุณตรงนี้ >>>

# -------------------------------------------------------------------
# (ไม่บังคับ) ลิงก์ดาวน์โหลด tunnel-client อัตโนมัติ
#
# ถ้าใส่บรรทัดนี้ ตัวติดตั้งจะดาวน์โหลด tunnel-client ให้เอง
# แล้วเก็บไว้ที่ %LOCALAPPDATA%\Happy Study\cache
# ครั้งต่อไปจะใช้ของที่เก็บไว้ ไม่โหลดซ้ำ
#
# ไฟล์ zip ต้องมี tunnel-client.exe อยู่ข้างใน
# และต้องกดโหลดได้ทันทีโดยไม่ต้องล็อกอิน
#
# รองรับลิงก์จาก GitHub Releases, Google Drive, Dropbox, HuggingFace
# -------------------------------------------------------------------
# tunnel client url : https://github.com/OWNER/REPO/releases/download/v1.0.0/tunnel-client.zip